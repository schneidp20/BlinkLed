# BlinkLed
Arduino Class library to animate WS2012 LEDs. Animation is encapsulated in the class so all you do is call the instance on each control loop. The class takes care of rest. Input and call sequence can be found in the header file. Requires adafruit neopixel library
